name: mail_core
description: Core data models and interfaces for the mail client.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  collection: ^1.19.1

dev_dependencies:
  build_runner: ^2.4.13
  freezed: ^2.5.8
  json_serializable: ^6.9.4
  test: ^1.25.15
